
bash_dev_env=$( dirname -- "${BASH_SOURCE[0]}" )

if [ -f $bash_dev_env/.bash_functions ]; then
    . $bash_dev_env/.bash_functions
fi

if [ -f $bash_dev_env/.bash_aliases ]; then
    . $bash_dev_env/.bash_aliases
fi
