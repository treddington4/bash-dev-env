
bash_dev_env=$( dirname -- "${BASH_SOURCE[0]}" )

if [ -f $bash_dev_env/functions_bash ]; then
    . $bash_dev_env/functions_bash
fi

if [ -f $bash_dev_env/aliases_bash ]; then
    . $bash_dev_env/aliases_bash
fi
